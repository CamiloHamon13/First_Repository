Este es un tercer archivo fuera de la carpeta antes de crear el repositorio


2 linea 2 commit Se realiza modificación luego de la creación del repositorio en Windows
3 commit, se cambia nombre del archivo a Vista01 y se inserta en la carpeta "Vista"

--> Se señala desde acá el HEAD o main

4 commit desde main, se adelanta proyecto general

5 commit desde main, se agrega esta línea para luego realizar MERGE inmediatamente luego de este commit, que nervios!!!

--> acá termina el HEAD o main e inicia lo que se realizó en rvista1

4 Commit pero 1 commit en la rama vista para realizar MERGE desde la rama main a la rama rvista

5 Commit, pero 2 commit en la rama vista
se agregan lineas en la rama rvista para luego hacer un MERGE hacia la rama main

En el 3 commit de la rvista se modifican las 2 lineas de arriba y se agrega esta línea :D

--> Acá termina lo realizado en la rama vista rvista

----------------------> No se borró lo realizado en la rama main ni en rama vista, se unificó

Este es el 7 commit, pero se notifica que en el sexto se realiza MERGE con la rvista
Se agregan dos lineas en el 7 commit de main. Acá va muriendo la rama rvista.

8 commit :o luego de este commit se realizará MERGE entre main y rcontrolador